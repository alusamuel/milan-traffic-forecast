# Figures

`figures/figures/` receives the distribution, five two-week series, top-area ACF, daily profile, and nine model/area forecast PNGs. All images are generated from the complete dataset by `python run.py eda` and `python run.py plots`. The committed images come from the full-data run; rerunning the pipeline regenerates them.
